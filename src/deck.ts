export default class Deck {
    addCard(name: string, quantity = 1) {}
    removeCard(name: string, quantity = 1) {}
    numberOfCards(): number { return -1 }
    numberOf(name: string): number { return -1 }
}